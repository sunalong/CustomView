package com.itcode.customView.view.matrix;

import android.content.Context;
import android.util.AttributeSet;
import android.widget.TextView;

/**
 * 可如同ImageView一样使用矩阵的TextView
 * Created by along on 16/7/18.
 */

public class MatrixTextView extends TextView {
    public MatrixTextView(Context context) {
        super(context);
    }

    public MatrixTextView(Context context, AttributeSet attrs) {
        super(context, attrs);
    }

    public MatrixTextView(Context context, AttributeSet attrs, int defStyle) {
        super(context, attrs, defStyle);
    }
}
